class Solution {
    static int setBits(int N) {
        // code here
        int count=0;
        while(N>0)
        {
            int rm=N%2;
            if(rm==1) count++;
            N=N/2;
        }
        return count;
    }
}
